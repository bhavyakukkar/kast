const fib = (n :: Int32) -> Int32 => (
    if less[Int32](n, 2) then (
        1
    ) else (
        add[Int32](fib(sub[Int32](n, 1)), fib(sub[Int32](n, 2)))
    )
);

const main = () -> () => (
    print_string("fib(10) = ");
    print_int32(fib(10));
    print_string("\n");
);
