const Int32 = @opaque_type;
const Bool = @opaque_type;
# const add = (a :: Int32, b :: Int32) -> Int32 => (
#     @native "\(a) + \(b)"
# );
# const sub = (a :: Int32, b :: Int32) -> Int32 => (
#     @native "\(a) - \(b)"
# );
# const less = (a :: Int32, b :: Int32) -> Bool => (
#     @native "\(a) < \(b)"
# );
# const fib = (n :: Int32) -> Int32 => (
#     if is_less(n, 2) then (
#         1
#     ) else (
#         add(fib(sub(n, 1)), fib(sub(n, 2)))
#     )
# );

const OptionTag = enum {
    | :Some
    | :None
};

const OptionData = union {
    .Some :: Int32,
    .None :: (),
};
# const main = () -> Unit => (
#     print("Hello, world!");
# );

const main = () -> () => (
    main;
);
